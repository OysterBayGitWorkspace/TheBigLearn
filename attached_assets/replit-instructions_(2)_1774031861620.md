# Replit Instructions — Termy Upgrade

Paste this into the Replit AI chat, with the App.jsx attached:

---

Replace `App.jsx` with the uploaded file, BUT with one critical merge step:

1. **Replace `App.jsx` entirely with the uploaded file FIRST.**

2. **Then copy the QUESTIONS array and RESOURCE_LIBRARY array from the OLD App.jsx back into the new file**, replacing the shortened versions. The new file has placeholder/shortened questions — the old file has the full 36 questions with complete explanations and germanContext fields. Those must be preserved exactly. The variable names and structure are identical, so it's a straight swap of those two arrays.

3. **Update `index.html`** — change the page title to "Termy — German VC Terms Trainer"

4. **Keep all other files unchanged** — `main.jsx`, `package.json`, `vite.config.js` stay as-is. No new dependencies.

5. **Do not refactor, reorganize, or "improve" any code.** The file is self-contained React 18 with no external deps.

6. **After replacing, confirm:**
   - Home screen shows "Termy" with gradient title
   - Animated fire streak with particles
   - Pulsing hearts
   - Rotating social pressure ticker
   - Shine animation on Start Training button
   - All 36 original questions work in training mode
   - Custom SVG sticker icons (no emojis anywhere)

Summary: new App.jsx for all the UI/game mechanics, but keep the original QUESTIONS and RESOURCE_LIBRARY data.

---
